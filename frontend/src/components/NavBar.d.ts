declare module './NavBar' {
    export interface NavBarProps {
      // declare the props that the NavBar component accepts
    }
    export default class NavBar extends React.Component<NavBarProps> {}
  }
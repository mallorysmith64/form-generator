declare module './CreateForm' {
    export interface CreateFormProps {
      // declare the props that the CreateForm component accepts
    }
    export default class CreateForm extends React.Component<CreateFormProps> {}
  }